<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pythonsrus.snakecatcher_refactor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraPreview" />
        <activity android:name=".SignIn" />
        <activity android:name=".Camera" />
        <activity android:name=".SettingsFragment" />
        <activity android:name=".ShowPhotoVideo" />
        <activity android:name=".GMAILSender" />
        <activity android:name=".JSSEProvider" />
        <activity android:name=".HistoryView" />
        <activity android:name=".Settings" />
        <activity android:name=".HistoryItemView"></activity>
    </application>

</manifest>